# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.11.0.20260408"

package:
  name: types-parsimonious
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/t/types-parsimonious/types_parsimonious-${{ version }}.tar.gz
  sha256: 868ad539881f915a99d2a457ac74c571b5c21dc7537d5657109bd77f123d8d97

build:
  number: 0
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - setuptools >=77.0.3
  run:
    - python >=${{ python_min }}

tests:
  - requirements:
      run:
        - mypy
        - parsimonious
        - python ${{ python_min }}.*
    script:
      - mypy -p parsimonious

about:
  summary: Typing stubs for parsimonious
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/python/typeshed/tree/main/stubs/parsimonious

extra:
  recipe-maintainers:
    - bollwyvl
